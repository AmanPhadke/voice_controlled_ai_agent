# voice_controlled_ai_agent
A local AI agent that can accept audio input, accurately classify the user's intent, execute the appropriate local tools, and display the entire pipeline's results in a clean User Interface (UI).
